# iwd is an optional Wi-Fi backend for NetworkManager (wpa_supplicant is
# the default). When the backend is switched to iwd via netoy-backend /
# the netoy-tui Network tab, iwd's own network configuration is left
# disabled — NetworkManager does the association and DHCP, so iwd only
# provides the link layer, exactly like wpa_supplicant does.
[General]
