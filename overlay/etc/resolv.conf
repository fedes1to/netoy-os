# /etc/resolv.conf - NetworkManager writes the router-provided DNS here.
# (A regular file, not a symlink: NM's DNS plugin refuses to write through
# a foreign symlink like the Alpine default -> /run/resolv.conf.)
